---
import i18next from "i18next";
import { t, changeLanguage } from "i18next";
import { localizePath } from "astro-i18next";
import SearchForm from "@components/SearchForm.astro";
import BaseLayout from "@layouts/Base.astro";
import Search from "@components/bikes/SearchBikes.astro";
import Card from "@components/statisticsCard.astro";
import Bike from "@components/Card.astro";
import HeroSection from "@components/sections/HeroSection.astro";
import BikesSection from "@components/sections/BikesSection.astro";

changeLanguage("en");
---

<BaseLayout title={t("homepage.title")} lang={i18next.language} description="">
  <HeroSection />

  <!-- Statistics -->
  <div class="flex justify-center gap-4">
    <Card key="320,000" value={t("homepage.stat1")} backColor="#E5EDFF" />
    <Card key="1,420,000" value={t("homepage.stat2")} backColor="#aec8ff" />
    <Card key="€5,6M" value={t("homepage.stat3")} backColor="#D5F5F6" />
  </div>

  <br />

  <BikesSection />

  <div
    class="grid sm:grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-2 justify-center"
  >
    <Bike lang="en" />
    <Bike lang="en" />
    <Bike lang="en" />
    <Bike lang="en" />
  </div>
  <br />

  <!--
          localizePath("/report/[id]").replace('[id]','224')
          <div
            class="input mt-10 mb-6 bg-white border-dashed border-gray-500 rounded border py-2 px-4"
          >
        <button class="flex gap-2 text-xs">
          <span class="font-bold">Drag a stolen bike image here</span> or <span
            class="text-green-500">upload a file</span
          >
          <img src="#" />
          Search by Image
        </button>
      </div>
      <div class="my-4">
        <span class="text-gray-400">No luck?</span> Try to listing your bike
      </div>
      <a
        class="list-button text-white bg-[#2DA771] rounded py-2 px-4 tracking-wider uppercase text-sm"
        href={localizePath("/create-report")}>{t("homepage.hero.cta_add_listing")}</a
      >
    </div>
    <div class="listing"></div>
    <div class="blog"></div>
     -->
</BaseLayout>
